alias \
  cp='cp -ir' \
  mv='mv -i' \
  rm='rm -I' \
  ln='ln -i' \
  df='df -hT' \
  free='free -h' \
  du='du -h' \
  tree='tree -C' \
  grep='grep --color=auto' \
  ls='ls -AGlht --color=auto --classify=auto' \
  ip='ip -c=auto' \
  mkdir='mkdir -p' \
  ps='ps aux' \
  less='less -F' \
  dd='dd status=progress' \
  uptime='uptime --pretty' \
  wget='wget -c' \
  diff='diff --color=auto -r' \
  history='history 1' \
  less='less -i' \
  cd=custom_cd \
  uname='uname -a'
